{
  "name": "guiyun-zhimai",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch server.mjs",
    "start": "node --env-file-if-exists=.env server.mjs",
    "check": "node --check server.mjs && node --check src/app.js && node --test tests/*.test.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
